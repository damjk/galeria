package Domain.Publicacion;

import Domain.Asociacion.Asociacion;
import Domain.Contacto.Contacto;
import Domain.Direccion.Direccion;
import Domain.Mascota.Especie;
import Domain.Mascota.Foto;
import Domain.Mascota.Mascota;
import Domain.Mascota.Sexo;
import Domain.Repositorio;
import Domain.Usuario.Persona;
import Domain.Usuario.TipoDocumento;
import org.junit.Assert;
import org.junit.Test;

import java.io.IOException;
import java.util.HashSet;
import java.util.Set;

public class TestPublicacionMascotaPerdida {
    /*
    @Test
    public void publicacionMascotaPerdida() throws IOException {

        PublicacionMascotaPerdida publicacionMascotaPerdida = Repositorio.getPublicacionMascotaPerdida();

        Assert.assertEquals("Alejandro", publicacionMascotaPerdida.getAutor().getNombre());
    }
    @Test
    public void mostrarPublicacionMascotaPerdida() throws IOException {
        PublicacionMascotaPerdida publicacionMascotaPerdida = Repositorio.getPublicacionMascotaPerdida();
        Assert.assertEquals("Encontramos a Dolca y busca duenio.", publicacionMascotaPerdida.mostrar().getTitulo());

    }

     */
}
